{
  "name": "@osrs-llm-helper/osrs-assets",
  "version": "0.0.0",
  "private": true,
  "description": "Catalog and helpers for OSRS sprite/asset URLs used by the marketing site and dashboard.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@osrs-llm-helper/tooling": "workspace:*",
    "typescript": "^5.7.2"
  }
}
