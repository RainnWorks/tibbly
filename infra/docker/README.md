# infra/docker

Local-development Docker Compose lives here. Backend agent (B1) will land:

- `docker-compose.yml` (PGLite/Postgres, backend, redis-if-needed)
- A `Dockerfile` per app where a deploy target needs it.

Placeholder until then.
