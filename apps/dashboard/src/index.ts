/**
 * @osrs-llm-helper/dashboard
 *
 * React + Vite + Tailwind portal for logged-in users. Placeholder export
 * exists so the workspace typechecks before the Dashboard agent (F1)
 * scaffolds the real Vite app.
 */
export const DASHBOARD_PLACEHOLDER = "osrs-llm-helper dashboard placeholder" as const;
